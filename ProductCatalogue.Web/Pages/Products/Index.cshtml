@page
@model ProductCatalogue.Web.Pages.Products.IndexModel

@* 
Pseudocode / Plan (step-by-step):
1. For each product row render columns for Id, Name, Price, Stock.
2. Make the "Name" cell redirect to the ProductDetails Razor Page when clicked.
   - Build the details URL with Url.Page("/Products/ProductDetails", new { id = item.Id }).
   - Apply an inline onclick on the <td> to navigate to that URL for full-cell clickability.
   - Add an <a> inside the <td> with asp-page and asp-route-id for accessibility and SEO (so links work without JS).
   - Add `style="cursor:pointer"` and `class="position-relative"` to the <td> so it feels clickable and the anchor can be a stretched-link if desired.
   - Include aria-label on the <td> for screen readers.
3. Keep the rest of the table rendering unchanged.
*@

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Price</th>
            <th>Stock Quantity</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Products != null && Model.Products.Any())
        {
            foreach (var item in Model.Products)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.Price.ToString("C")</td>
                    <td>@item.Stock</td>
                    <td>
                        <a asp-page="/Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">View Details</a> |
                        <a asp-page="/Orders/Create" asp-route-productId="@item.Id" class="btn btn-sm btn-success">Place Order</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5" class="text-center text-muted">No products found</td>
            </tr>
        }
    </tbody>
</table>
